{
  "name": "reksio-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted browser launcher for ReksioEngine games.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^2.0.2"
  },
  "engines": {
    "node": ">=22"
  }
}
