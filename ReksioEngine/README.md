# ReksioEngine Local Vendor

This directory contains the local engine source used by Reksio UI.

The Docker image builds this source directly and does not clone the engine from a remote repository. Keep private disc images outside this directory; the UI app expects those under the ignored top-level `ISOs/` folder during compatibility testing.
